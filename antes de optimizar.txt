/* Variables CSS */
:root {
  --color-primario: #1e90ff;
  --color-secundario: #241824;
  --color-texto: #923986;
  --fuente-principal: 'Poppins', sans-serif;
  
}

/* Reset y estilos generales */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: var(--fuente-principal);
  color: var(--color-texto);
  background: url('https://wallpapers.com/images/hd/purple-pink-background-3fnuodzynrw7paw8.jpg') no-repeat center center/cover;
}

/* Barra de navigacion*/
.navbar {
  background-color: rgb(111, 27, 150);
  padding: 10px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  flex-wrap: wrap;
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s ease;
}

.navbar .logo {
  color: white;
  font-size: 24px;
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 16px;
}

.nav-links a {
  color: white;
  text-decoration: none;
  padding: 12px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.nav-links a:hover {
  background-color: #f843e9;
}

/* Hero */
.hero {
  background: url('https://wallpapers.com/images/hd/purple-gaming-gesqfhppt23032mg.jpg') no-repeat center/cover;
  height: 70vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  color: white;
  padding: 0 16px;
}

.hero h1 {
  font-size: 48px;
  position: absolute;
  top: 100px;
  left: 50%;
  transform: translateX(-50%);
  color: transparent;
  background: linear-gradient(45deg, #faf7fa, #f843e9);
  -webkit-background-clip: text;
  z-index: 1;
  font-weight: bold;
  text-shadow: 3px 3px 5px rgba(0, 0, 0, 0.5);
}

.hero-content p {
  font-size: 24px;
  margin: 16px 0;
}

/* Slider */
.slider-container {
  position: relative;
  top: 70px;
  width: 100%;
  max-width: 800px;
  margin: 32px auto;
}

.slider {
  display: flex;
  overflow: hidden;
}

.slide {
  width: 100%;
  display: none;
}

.slide.active {
  display: block;
}

/* Flechas y puntos de navegación */
.prev, .next {
  position: absolute;
  top: 50%;
  transform: translateY(-50%);
  background-color: rgba(0, 0, 0, 0.5);
  color: white;
  border: none;
  padding: 16px;
  font-size: 32px;
  cursor: pointer;
}

.prev { left: 0; }
.next { right: 0; }

.prev:hover, .next:hover {
  background-color: rgba(0, 0, 0, 0.8);
}

.lineas {
  position: absolute;
  bottom: 16px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
}

.linea {
  height: 2px;
  width: 20px;
  margin: 0 6px;
  border-radius: 50%;
  background-color: white;
  opacity: 0.6;
  cursor: pointer;
}

.linea.active {
  background-color: #f1f1f1;
  opacity: 1;
}

/* Categorías, productos destacados y CTA */
.categorias, .productos-destacados, .cta {
  padding: 32px;
  text-align: center;
}

.categoria, .producto {
  background-color: white;
  padding: 16px;
  border-radius: 8px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
  margin: 16px 0;
}

.categorias h2, .producto h3 {
  font-size: 24px;
}

.productos-destacados {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 16px;
}

.productos-destacados h2 {
  width: 100%;
  margin-bottom: 20px;
}

.producto {
  width: 280px;
  text-align: center;
}

.producto img {
  width: 100%;
  border-radius: 8px;
}

.boton {
  background-color: rgb(111, 27, 150);
  color: white;
  padding: 12px 24px;
  text-decoration: none;
  border-radius: 5px;
  display: inline-block;
  margin-top: 16px;
}

.boton:hover {
  background-color: #f843e9;
}

/* Footer */
.footer {
  background-color: black;
  color: white;
  text-align: center;
  padding: 16px;
  margin-top: 32px;
}

/* Página Producto */
.detalle-producto {
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  padding: 32px;
}

.detalle-producto img {
  width: 300px;
  border-radius: 8px;
}

.precio {
  font-size: 24px;
  color: green;
  margin: 16px 0;
}

/* Formulario Contacto */
.formulario-contacto {
  padding: 32px;
  max-width: 500px;
  margin: 0 auto;
}

form {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

input, textarea {
  padding: 8px;
  border: 1px solid #ccc;
  border-radius: 5px;
}

/* Media Queries - Responsive */
@media (max-width: 768px) {
  body { font-size: 14px; }

  .navbar { flex-direction: column; gap: 8px; }
  .navbar .logo { font-size: 20px; }

  .hero h1 { font-size: 28px; }
  .hero-content p { font-size: 14px; }

  .slider-container { max-width: 100%; }
  .prev, .next { padding: 12px; font-size: 24px; }

  .categorias, .cta { padding: 24px; }
  .categoria { width: 100%; }

  .productos-destacados { flex-direction: column; }
  .producto { width: 80%; margin: 0 auto; }

  .boton { padding: 10px 20px; }

  .footer { padding: 12px; }

  .detalle-producto {
    flex-direction: column;
    padding: 16px;
  }

  .detalle-producto img { width: 100%; }

  .formulario-contacto { padding: 16px; }
  input, textarea { font-size: 14px; }
}
/* Contenedor de productos destacados */
.productos-destacados {
  display: flex;
  flex-direction: column; /* Ahora apila galería + productos */
  align-items: center;     /* Centra todo horizontalmente */
  padding: 32px;
}

/* Galería */
.productos-destacados .galeria {
  display: flex;
  width: 600px;
  height: 430px;
  margin-bottom: 32px; /* Espacio entre galería y productos */
  justify-content: center; /* Centrar las imágenes dentro */
}

/* Tamaño galería */
.productos-destacados .galeria img {
  width: 0px;
  flex-grow: 3;
  object-fit: cover;
  opacity: .5;
  transition: .5s ease;
}

.productos-destacados .galeria img:hover {
  cursor: crosshair;
  width: 300px;
  opacity: 1;
  filter: contrast(120%);
}

.productos-destacados .galeria img:first-child {
  width: 100px;
  opacity: 1;
  filter: contrast(120%);
}
/* Responsive para teléfono */
@media (max-width: 768px) {
  .productos-destacados .galeria {
    width: 100%;
    height: auto;
    flex-direction: column;
  }
  .productos-destacados .galeria img {
    width: 100%;
    flex-grow: 0;
    opacity: 1;
  }
  .productos-destacados .galeria img:hover {
    width: 100%;
  }
  .productos-destacados .galeria img:first-child {
    width: 100%;
  }
}